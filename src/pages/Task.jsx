import React from "react";
import SimplePage from "./SimplePage";
export default function Task() {
  return <SimplePage name="Task" bullets={["Return preparation", "Ledger review", "Client follow-up", "Partner approval"]} />;
}

