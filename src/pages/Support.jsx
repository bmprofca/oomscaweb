import React from "react";
import SimplePage from "./SimplePage";
export default function Support() {
  return <SimplePage name="Support" bullets={["Raise an issue", "Escalate a filing concern", "Check SLA status", "Contact the back office"]} />;
}

